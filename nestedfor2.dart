// write a programe to print half inverted static line pyramid
import 'dart:io';

void main() {
  int count = 0,flash=5;

  for(flash=5;flash>0;flash--)
  {
    for (count = 0; count < flash; count++)
     stdout.write(flash);
    stdout.write('\n');
  }
  // for(count=0 ; count<5 ; count++)
  //   stdout.write('*');
  // stdout.write('\n');

  // for(count=0 ; count<5 ; count++)
  //   stdout.write('*');
  // stdout.write('\n');

  // for(count=0 ; count<5 ; count++)
  //   stdout.write('*');
  // stdout.write('\n');

  // for(count=0 ; count<5 ; count++)
  //   stdout.write('*');
  // stdout.write('\n');
}
